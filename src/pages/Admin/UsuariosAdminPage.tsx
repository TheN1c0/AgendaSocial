import { Card } from '../../components/ui/Card';

export const UsuariosAdminPage = () => {
  return (
    <div>
      <h1 style={{ marginTop: 0, marginBottom: '2rem' }}>Gestión de Usuarios</h1>
      <Card>
        <p>Panel de administración de cuentas para perfiles con rol de "admin".</p>
      </Card>
    </div>
  );
};
